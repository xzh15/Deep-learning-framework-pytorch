# 神经网络基础

> 参考课程：黑马程序员《神经网络与深度学习》
> B站视频：https://www.bilibili.com/video/BV1c5yrBcEEX/
> 用于自学记录

---

## 目录

- [一、神经网络介绍](#一神经网络介绍)
- [二、激活函数](#二激活函数)
- [三、参数初始化](#三参数初始化)
- [四、搭建神经网络](#四搭建神经网络)
- [五、损失函数](#五损失函数)
- [六、网络优化方法](#六网络优化方法)
- [七、正则化方法](#七正则化方法)
- [八、案例——手机价格分类](#八案例手机价格分类)

---

## 一、神经网络介绍

**神经网络（Neural Network，NN）** 也叫 **人工神经网络（Artificial Neural Network，ANN）**，是一种模仿生物神经网络结构和功能的**计算模型**。它由大量相互连接的**神经元（Neuron）** 组成，通过调整连接之间的**权重（Weight）**，实现对复杂非线性函数的拟合。

### 1.1 神经网络

神经网络是由多个神经元组成，构建神经网络就是在构建神经元。

**神经元的工作原理：**

将输入层的数据进行加权求和，通过激活函数（如 Sigmoid）将结果映射到 [0, 1] 之间。

线性公式：

```
y = wx + b
```

```
f(b + Σ(i=1→n) xᵢwᵢ)
```

- 有几个特征，输入层就有几个神经元
- 同一层的多个神经元可以看作是通过并行计算来处理相同的输入数据

**全连接神经网络（FCNN）：**

- **相邻层之间的神经元相互连接**：第 N 层的每个神经元和第 N-1 层的所有神经元相连
- 全连接神经网络接收的样本数据是**二维的**，数据在每一层之间需要以二维的形式传递
- 每个连接都有一个权重值（w 系数和 b 系数）
- **同一层神经元相互隔离没有连接**，前一层的输出 = 后一层的输入

**神经网络的基本组成部分：**

| 层 | 说明 |
|---|---|
| **输入层（Input Layer）** | 即输入 x 的那一层。每个输入特征对应一个神经元。不进行计算，仅传递数据 |
| **输出层（Output Layer）** | 即输出 y 的那一层。输出最终预测结果（类别/数值），结构取决于任务：回归、分类 |
| **隐藏层（Hidden Layers）** | 输入层和输出层之间都是隐藏层，神经网络的"深度"通常由隐藏层的数量决定。自动提取特征、进行非线性变换，层数越多，网络越"深"，表达能力越强 |

神经网络中信息只向一个方向移动，即从输入节点向前移动，通过隐藏节点，再向输出节点移动。

---

## 二、激活函数

**激活函数（Activation Function）** 是神经网络中的一种数学函数，它接收上一层神经元的加权输入，并**决定该神经元是否应该被"激活"**（即是否将信号传递给下一层），进而为整个网络注入了**非线性因素**。此时，神经网络就可以拟合各种曲线，这样就可以来做分类问题了。

> 如果没有激活函数，无论神经网络有多少层，最终都等价于一个线性变换，这将使网络完全失效。

### 2.1 常见的激活函数

#### 2.1.1 Sigmoid 激活函数

**公式：**

```
f(x) = 1 / (1 + e⁻ˣ)
```

**求导：**

```
f'(x) = f(x)·(1 - f(x))
```

**函数与导数图像：**
- 函数输出范围：(0, 1)
- 导数范围：[0, 0.25]，在 x=0 处取得最大值 0.25

**优点：**

| 优点 | 说明 |
|---|---|
| **平滑且可导** | 处处可微，适合基于梯度的优化算法（SGD、Adam 等） |
| **输出范围固定** | 输出在 (0, 1) 之间，非常适合**表示概率** |
| **导数简单** | `f'(x) = f(x)(1-f(x))`，反向传播计算高效 |
| **解释性强** | 二分类任务中，输出可以直接解释为"属于正类的概率" |

**缺点：**

| 缺点 | 说明 | 后果 |
|---|---|---|
| **梯度消失** | 当输入非常大或非常小时，梯度趋近于 0 | 深层网络中，梯度在反向传播时指数级衰减，导致靠前的层几乎无法更新 |
| **非零中心** | 输出均值恒为 0.5，而非 0 | 梯度更新时，所有权重的梯度符号相同，导致 **"Z 型更新"**（zig-zag），收敛变慢 |
| **计算开销大** | 涉及指数运算 `e^(-x)` | 相比 ReLU 的 `max(0, x)` 更耗时 |
| **软饱和** | 即使输入变化很大，输出变化也很小 | 神经元在饱和区对输入不敏感，信息流动受阻 |

**使用场景：**

| 场景 | 原因 |
|---|---|
| **二分类输出层** | 输出在 (0,1)，可直接作为正类概率 |
| **多标签分类输出层** | 每个神经元独立输出 0~1 概率（一个样本可同时属于多个类别） |
| **注意力机制** | 生成 0~1 之间的注意力权重 |
| **门控机制** | LSTM / GRU 中的"遗忘门"、"输入门"等，控制信息流动比例 |
| **概率映射** | 将任意数值映射为概率值（如强化学习中的策略网络） |

---

#### 2.1.2 Tanh 激活函数

**Tanh（Hyperbolic Tangent，双曲正切）** 函数是神经网络中经典的激活函数之一。它是 Sigmoid 函数的**缩放平移版本**，将任意实数输入映射到 **(-1, 1)** 区间，且以 0 为中心。

**公式：**

```
f(x) = (eˣ - e⁻ˣ)/(eˣ + e⁻ˣ) = 2/(1 + e⁻²ˣ) - 1
```

**求导：**

```
f'(x) = 1 - f²(x)
```

**函数与导数图像：**
- 函数输出范围：(-1, 1)
- 导数范围：[0, 1]，在 x=0 处取得最大值 1

**优点：**

| 优点 | 说明 |
|---|---|
| **零中心** | 输出均值为 0，收敛速度比 Sigmoid 快 |
| **梯度更大** | 最大导数为 1（Sigmoid 仅 0.25），梯度传播更顺畅 |
| **平滑且可导** | 处处可微，适合基于梯度的优化算法 |
| **导数简单** | `f'(x) = 1 - f²(x)`，反向传播计算高效 |
| **对称性** | 奇函数：`f(-x) = -f(x)`，利于建模正负信号 |

**缺点：**

| 缺点 | 说明 | 后果 |
|---|---|---|
| **梯度消失** | 当输入过大或过小时，梯度趋近于 0 | 深层网络中仍存在梯度消失问题（但比 Sigmoid 轻） |
| **计算开销较大** | 涉及指数运算 `e^x` 和 `e^(-x)` | 慢于 ReLU 的简单比较 |
| **软饱和** | 在两端（正负无穷）对输入不敏感 | 信息流动受阻 |

**使用场景：**

| 场景 | 原因 |
|---|---|
| **RNN / LSTM 隐藏层** | 零中心、梯度比 Sigmoid 更稳定，适合处理时间序列 |
| **GAN 生成器输出层** | 输出范围 (-1, 1)，与图像归一化范围匹配 |
| **需要零中心数据的场景** | 输出均值为 0，利于下一层学习 |
| **早期的浅层网络** | 在 ReLU 流行之前，Tanh 是隐藏层的常用选择 |
| **特征标准化层** | 将特征映射到 (-1, 1) 区间 |

---

#### 2.1.3 ReLU 激活函数

**ReLU（Rectified Linear Unit，修正线性单元）** 是当今深度学习中**最主流、最常用**的激活函数。它简单到极致——正数保留，负数归零。

**公式：**

``f(x) = max(0, x)``

ReLU 在 `x=0` 处不可导，但工程上通常约定其导数为：

```
f'(x) = { 0,  x < 0
         { 1,  x ≥ 0
```

**函数与导数图像：**
- 函数输出范围：[0, +∞)
- 导数：正区间为 1，负区间为 0

**优点：**

| 优点 | 说明 |
|---|---|
| **计算极快** | 只需比较 `max(0, x)`，无需指数运算（远快于 Sigmoid/Tanh） |
| **缓解梯度消失** | 正区间梯度恒为 1，不会像 Sigmoid/Tanh 那样在两端衰减 |
| **稀疏激活** | 负数全部归零，约 50% 的神经元输出为 0，减少计算量 |
| **促进稀疏性** | 稀疏性意味着更少的参数参与计算，有助于提取更鲁棒的特征 |
| **生物合理性** | 更接近生物神经元的"全有或全无"发放模式 |

**缺点：**

| 缺点 | 说明 | 后果 |
|---|---|---|
| **神经元死亡（Dying ReLU）** | 如果输入始终为负，梯度为 0，神经元将永远无法激活 | 大量死亡神经元导致模型容量下降 |
| **非零中心** | 输出均值为正（约 0.5），而非 0 | 可能导致梯度更新不稳定 |
| **负值截断** | 负值信息完全丢失 | 无法表达"负信号"的意义（如抑制效果） |

> **神经元死亡**：当一个神经元的输入 x 在训练中始终小于 0，它的梯度恒为 0，权重永远无法更新。这个神经元就"死了"——它对任何输入都输出 0。

---

#### 2.1.4 SoftMax 激活函数

**SoftMax** 是一种专门用于**多分类**任务的激活函数，通常放在神经网络的**输出层**。它将一个 K 维的实数向量 `z1, z2, ..., zK` 转换为一个**概率分布**——每个输出值都在 (0, 1) 之间，且所有输出之和为 1。

**公式：**

```
Softmax(zᵢ) = e^zᵢ / Σ(j=1→K) e^zⱼ ,  i = 1, 2, ..., K
```

| 符号 | 含义 |
|---|---|
| `K` | 类别总数 |
| `zᵢ` | 第 i 个类别的原始输出（logit） |
| `e^zᵢ` | 对 logit 取指数（保证为正数） |
| `Σⱼ=1^K e^zⱼ` | 所有类别的指数之和（归一化分母） |

**示例：** 输入 logits 为 [0.2, 0.02, 0.15, 0.15, 1.3, 0.5, 0.06, 1.1, 0.05, 3.75]，经过 Softmax 后输出概率分布，最大的 3.75 对应 74% 的概率，所有概率之和为 1。

---

#### 2.1.5 其他激活函数

| 名称 | 公式 | 导数 | 范围 |
|---|---|---|---|
| **Identity（恒等）** | `f(x) = x` | `f'(x) = 1` | `(-∞, +∞)` |
| **Binary step（阶跃）** | `f(x) = {0, x<0; 1, x≥0}` | `x≠0` 时为 0 | {0, 1} |
| **Leaky ReLU** | `f(x) = {0.01x, x<0; x, x≥0}` | `f'(x) = {0.01, x<0; 1, x≥0}` | `(-∞, +∞)` |
| **PReLU（参数化ReLU）** | `f(x,α) = {α·x, x<0; x, x≥0}` | `α` 可学习 | `(-∞, +∞)` |
| **ELU** | `f(x,α) = {α·(e^x-1), x<0; x, x≥0}` | `f'(x,α) = {f(x,α)+α, x<0; 1, x≥0}` | `(-α, +∞)` |

### 2.2 激活函数的选择

| 位置 | 场景 | 首选 | 备选 |
|---|---|---|---|
| **隐藏层** | 默认选择 | **ReLU** | Leaky ReLU |
| | 神经元死亡严重 | **Leaky ReLU** | PReLU / ELU |
| | Transformer | **GELU** | Swish |
| | RNN / LSTM | **Tanh** | ELU |
| | GAN 判别器 | **Leaky ReLU** | ReLU |
| **输出层** | 二分类 | **Sigmoid** | — |
| | 多分类（互斥） | **Softmax** | — |
| | 多标签分类 | **Sigmoid**（每个节点） | — |
| | 回归 | **无（线性）** | — |

---

## 三、参数初始化

构建网络之后，网络中的参数需要初始化。需要初始化的参数主要有**权重**和**偏置**，**偏置一般初始化为 0 即可**，而对权重的初始化则更加重要。

参数初始化就是"给模型一个**起点**"，决定了模型从何处开始寻找最优解。

| 初始化质量 | 对训练的影响 |
|---|---|
| **好的初始化** | 模型从一开始就处于"合适的区域"，梯度适中，收敛快速 |
| **坏的初始化** | 模型起点偏差过大，梯度消失或爆炸，需要极长的训练时间甚至无法收敛 |

**对梯度的影响：**

| 问题 | 与初始化的关系 |
|---|---|
| **梯度消失** | 权重初始化太小 → 信号逐层衰减 → 梯度指数级变小 |
| **梯度爆炸** | 权重初始化太大 → 信号逐层放大 → 梯度指数级变大 |
| **神经元死亡（ReLU）** | 权重初始化不当 → 大量输入落入负区间 → 梯度为 0 |

**打破对称性：**

| 情况 | 后果 |
|---|---|
| **所有权重初始化为相同值** | 同一层的所有神经元学习完全相同的特征，相当于"只有一个神经元"在工作 |
| **随机初始化（打破对称）** | 每个神经元朝向不同的方向学习，分工合作，提高模型容量 |

### 3.1 参数初始化方法

| 初始化方法 | 初始化方式 | 优点 | 缺点 | 适用场景 |
|---|---|---|---|---|
| **随机初始化（均匀分布）** | 从均匀分布中随机采样 `W ~ U(-1/√(d), 1/√(d))`，d 为输入神经元数量 | 能有效打破对称性 | 随机范围选择不当可能导致梯度消失/爆炸 | 浅层网络（1-3层隐藏层） |
| **随机初始化（正态分布）** | 从高斯分布中随机采样 `W ~ N(0, σ²)`，常用 `σ=0.01` | 能有效打破对称性 | 标准差选择不当可能导致梯度问题 | 浅层网络或低复杂度模型 |
| **全0初始化** | 所有权重设为 0 | 实现简单 | **无法打破对称性**，所有神经元同步更新 | 仅用于偏置项初始化 |
| **全1初始化** | 所有权重设为 1 | 实现简单 | (1) 无法打破对称性 (2) 激活值指数级增长 → 梯度爆炸 | 仅用于测试/调试 |
| **Xavier / Glorot 初始化** | 方差基于输入输出维度，保持前向和反向传播的方差一致 | 有效缓解梯度消失 | 对 ReLU 激活函数**效果欠佳** | 深度网络，使用 **Sigmoid / Tanh** |
| **Kaiming / He 初始化** | 方差基于输入维度，专为 ReLU 设计 | **专为 ReLU 设计**，补偿方差减半，梯度稳定 | 对非 ReLU 激活函数效果一般 | 深度网络，使用 **ReLU / Leaky ReLU** |

#### 3.1.1 Kaiming / He 初始化

- **提出者**：何恺明 et al.（2015年提出）
- **适用**：**ReLU 及其变体**（如 Leaky ReLU）
- **目标**：解决 ReLU 将负值置零导致的方差损失问题

**正态分布版本（最常用）：**

```
W ~ N(0, 2/d_in)
```

**均匀分布版本：**

```
W ~ U(-√(6/d_in), √(6/d_in))
```

其中 `d_in` 表示当前层**输入神经元**的数量。

#### 3.1.2 Xavier / Glorot 初始化

**Xavier 初始化**（也称 **Glorot 初始化**）是一种权重初始化方法。它是专门针对 **Sigmoid** 和 **Tanh** 等"S 型"激活函数设计的，旨在解决深层网络中的**梯度消失**和**梯度爆炸**问题。

| 分布类型 | 公式 | 适用场景 |
|---|---|---|
| **正态分布** | `W ~ N(0, 2/(d_in + d_out))` | 通用，推荐使用 |
| **均匀分布** | `W ~ U(-√(6/(d_in+d_out)), √(6/(d_in+d_out)))` | PyTorch 默认实现之一 |

其中 `d_in` 为当前层输入神经元数量，`d_out` 为当前层输出神经元数量。

**PyTorch 初始化代码示例：**

```python
import torch.nn as nn

# 1. 均匀分布随机初始化
def test01():
    linear = nn.Linear(5, 3)
    nn.init.uniform_(linear.weight)
    nn.init.uniform_(linear.bias)
    print(linear.weight.data)

# 2. 固定初始化
def test02():
    linear = nn.Linear(5, 3)
    nn.init.constant_(linear.weight, 5)
    print(linear.weight.data)

# 3. 全0初始化
def test03():
    linear = nn.Linear(5, 3)
    nn.init.zeros_(linear.weight)
    print(linear.weight.data)

# 4. 全1初始化
def test04():
    linear = nn.Linear(5, 3)
    nn.init.ones_(linear.weight)
    print(linear.weight.data)

# 5. 正态分布随机初始化
def test05():
    linear = nn.Linear(5, 3)
    nn.init.normal_(linear.weight, mean=0, std=1)
    print(linear.weight.data)

# 6. Kaiming 初始化
def test06():
    linear = nn.Linear(5, 3)
    # kaiming 正态分布初始化
    nn.init.kaiming_normal_(linear.weight, nonlinearity='relu')
    print(linear.weight.data)
    # kaiming 均匀分布初始化
    linear = nn.Linear(5, 3)
    nn.init.kaiming_uniform_(linear.weight, nonlinearity='relu')
    print(linear.weight.data)

# 7. Xavier 初始化
def test07():
    linear = nn.Linear(5, 3)
    # xavier 正态分布初始化
    nn.init.xavier_normal_(linear.weight)
    print(linear.weight.data)
    # xavier 均匀分布初始化
    linear = nn.Linear(5, 3)
    nn.init.xavier_uniform_(linear.weight)
    print(linear.weight.data)
```

---

## 四、搭建神经网络

### 4.1 搭建神经网络的过程

#### 4.1.1 数据准备
- 准备训练数据集和测试数据集
- 使用 `DataLoader` 进行批次加载

#### 4.1.2 模型构建
- 继承 `nn.Module` 自定义模型类
- 在 `__init__` 中定义网络层（线性层、激活函数等）
- 在 `forward` 方法中定义前向传播过程

#### 4.1.3 训练配置
- 定义损失函数（如 `nn.CrossEntropyLoss`）
- 定义优化器（如 `optim.SGD`、`optim.Adam`）

#### 4.1.4 训练循环

| 步骤 | 操作 | 关键代码 |
|---|---|---|
| 1. 清空梯度 | 防止上一轮的梯度累积 | `optimizer.zero_grad()` |
| 2. 前向传播 | 将数据传入模型得到预测结果 | `outputs = model(inputs)` |
| 3. 计算损失 | 用损失函数计算预测与真值的误差 | `loss = criterion(outputs, labels)` |
| 4. 反向传播 | 计算当前梯度的值 | `loss.backward()` |
| 5. 更新参数 | 根据梯度下降更新权重 | `optimizer.step()` |
| 6.（可选）调整学习率 | 按调度器更新 | `scheduler.step()` |

#### 4.1.5 验证与评估
- 使用 `model.eval()` 切换到评估模式
- 在 `torch.no_grad()` 上下文中进行预测
- 计算准确率等评估指标

#### 4.1.6 保存与加载
- 保存：`torch.save(model.state_dict(), 'model.pth')`
- 加载：`model.load_state_dict(torch.load('model.pth'))`

### 4.2 案例：构建简单神经网络

**需求：**
- 第1个隐藏层：输入特征 3，输出特征 3，Xavier 初始化，Sigmoid 激活
- 第2个隐藏层：输入特征 3，输出特征 2，Kaiming 初始化，ReLU 激活
- 输出层：输入特征 2，输出特征 2，Softmax 激活

```python
import torch
import torch.nn as nn
from torchsummary import summary

# VS Code 终端默认编码不是 UTF-8
import sys
sys.stdout.reconfigure(encoding='utf-8')

# 创建神经网络模型类，自定义继承 nn.Module
class Model(nn.Module):
    # 1. 在 __init__() 方法中完成初始化：父类成员和神经网络的搭建
    def __init__(self):
        # 1.1 初始化父类成员
        super(Model, self).__init__()
        # 1.2 搭建神经网络的隐藏层、输出层
        # 1.2.1 第一个隐藏层：输入特征3，输出特征3，激活函数 Sigmoid
        self.linear1 = nn.Linear(3, 3)
        # 1.2.2 第二个隐藏层：输入特征3，输出特征2，激活函数 ReLU
        self.linear2 = nn.Linear(3, 2)
        # 1.2.3 输出层：输入特征2，输出特征2，激活函数 Softmax
        self.out = nn.Linear(2, 2)
        # 1.3 对隐藏层进行参数初始化
        nn.init.xavier_normal_(self.linear1.weight)
        nn.init.zeros_(self.linear1.bias)
        nn.init.kaiming_normal_(self.linear2.weight, nonlinearity='relu')
        nn.init.zeros_(self.linear2.bias)

    # 2. 创建前向传播方法，从输入层 -> 隐藏层 -> 输出层
    # 函数名称必须叫 forward，调用神经网络模型对象时自动执行 forward() 方法
    def forward(self, x):
        # 2.1 第一层隐藏层计算：加权求和 + 激活函数(Sigmoid)
        x = self.linear1(x)
        x = torch.sigmoid(x)
        # 2.2 第二层隐藏层计算：加权求和 + 激活函数(ReLU)
        x = torch.relu(self.linear2(x))
        # 2.3 输出层计算：加权求和 + 激活函数(Softmax)
        x = torch.softmax(self.out(x), dim=-1)  # dim=-1 表示按照最后一个维度进行计算
        return x

# 模型训练
def train():
    # 获取当前可用设备
    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    print(f"使用设备: {device}")

    # 1. 实例化 model 对象
    my_model = Model().to(device)

    # 2. 创建数据集样本（随机生成）
    data = torch.randn(5, 3).to(device)  # 5个样本，每个样本3个特征
    print("data-->", data)
    print("data shape", data.shape)

    # 3. 调用神经网络模型进行前向传播
    output = my_model(data)
    print("output-->", output)
    print("output shape-->", output.shape)

    # 4. 计算和查看模型参数
    print("======计算模型参数======")
    my_model_cpu = my_model.to("cpu")
    summary(my_model_cpu, input_size=(3,), batch_size=5, device='cpu')
    my_model = my_model_cpu.to(device)

    # 5. 查看模型参数
    print("======查看模型参数w和b======")
    for name, param in my_model.named_parameters():
        print(name, param.data)

if __name__ == "__main__":
    train()
```

---

## 五、损失函数

损失函数在不同文献中有不同命名：**损失函数（Loss Function）**、**代价函数（Cost Function）**、**目标函数（Objective Function）**。

在深度学习中，损失函数用来衡量模型参数质量，衡量的方式是比较网络输出（预测值）和真实输出（真实值）的差异。模型通过**最小化损失函数**的值来调整参数，使其输出更接近真实值。

### 5.1 分类任务损失函数

#### 5.1.1 二分类交叉熵

在处理二分类任务时，输出层使用 Sigmoid 激活函数，损失函数使用二分类交叉熵损失函数。

**公式：**

```
L = -(1/N)·Σ(i=1→N) [ yᵢ·log(pᵢ) + (1-yᵢ)·log(1-pᵢ) ]
```

| 符号 | 含义 |
|---|---|
| `N` | 样本总数 |
| `yᵢ` | 第 i 个样本的真实标签（0 或 1） |
| `pᵢ` | 模型预测样本 i 为正类的概率 |

**推导过程：**

对于第 i 个样本，真实标签为 `yᵢ`，模型预测正类概率为 `pᵢ`。模型认为该样本属于真实类别的概率可以统一写成：

```
P(yᵢ|pᵢ) = pᵢ^yᵢ · (1-pᵢ)^(1-yᵢ)
```

假设所有样本**独立同分布**，整个数据集的**联合概率（似然函数）**为：

```
L = ∏(i=1→N) pᵢ^yᵢ · (1-pᵢ)^(1-yᵢ)
```

取负对数（取平均）：

```
L = -(1/N)·Σ(i=1→N) [ yᵢ·log(pᵢ) + (1-yᵢ)·log(1-pᵢ) ]
```

**PyTorch 代码实现：**

```python
import torch
from torch import nn

def test_bce():
    # 1. 设置真实值和预测值
    y_true = torch.tensor([0, 1, 0], dtype=torch.float32)
    # 预测值是 sigmoid 输出的结果
    y_pred = torch.tensor([0.6901, 0.5459, 0.2469], requires_grad=True)

    # 2. 二分类交叉熵损失
    criterion = nn.BCELoss()
    loss = criterion(y_pred, y_true)
    print("BCE Loss:", loss.item())

    # 带 logits 的版本（内部自动计算 sigmoid，更稳定）
    logits = torch.tensor([0.8, 0.2, -1.1], requires_grad=True)
    criterion2 = nn.BCEWithLogitsLoss()
    loss2 = criterion2(logits, y_true)
    print("BCEWithLogits Loss:", loss2.item())
```

#### 5.1.2 多分类交叉熵

在处理多分类任务时，输出层通常不手动加 Softmax（因为 `nn.CrossEntropyLoss` 内部已包含 Softmax），直接使用多分类交叉熵损失函数。

**公式：**

```
L = -(1/N)·Σ(i=1→N) Σ(c=1→C) yᵢc·log(pᵢc)
```

| 符号 | 含义 |
|---|---|
| `N` | 样本总数 |
| `C` | 类别总数 |
| `y_ic` | 第 i 个样本是否属于类别 c 的真实值（one-hot 编码） |
| `p_ic` | 模型预测样本 i 属于类别 c 的概率 |

**PyTorch 代码实现：**

```python
import torch
from torch import nn

def test_ce():
    # 真实标签（类别索引，不需要 one-hot）
    y_true = torch.tensor([0, 2, 1])
    # 模型输出的 logits（未经过 softmax）
    logits = torch.tensor([
        [2.0, 1.0, 0.1],
        [0.5, 1.0, 2.5],
        [1.0, 3.0, 0.2]
    ], requires_grad=True)

    # 多分类交叉熵损失（内部自动计算 log_softmax）
    criterion = nn.CrossEntropyLoss()
    loss = criterion(logits, y_true)
    print("CrossEntropy Loss:", loss.item())
```

### 5.2 回归任务损失函数

#### 5.2.1 均方误差 MSE

**公式：**

```
L = (1/N)·Σ(i=1→N) (yᵢ - ŷᵢ)²
```

**PyTorch 代码：**

```python
criterion = nn.MSELoss()
loss = criterion(y_pred, y_true)
```

#### 5.2.2 平均绝对误差 MAE

**公式：**

```
L = (1/N)·Σ(i=1→N) |yᵢ - ŷᵢ|
```

**PyTorch 代码：**

```python
criterion = nn.L1Loss()
loss = criterion(y_pred, y_true)
```

#### 5.2.3 Smooth L1 损失函数

Smooth L1 结合了 MSE 和 MAE 的优点，在误差小时使用 MSE（平滑），误差大时使用 MAE（对异常值鲁棒）。

**公式：**

```
L = { 0.5x²,   |x| < 1
    { |x| - 0.5, |x| ≥ 1
```

其中 `x = yᵢ - y}ᵢ`

**PyTorch 代码：**

```python
criterion = nn.SmoothL1Loss()
loss = criterion(y_pred, y_true)
```

---

## 六、网络优化方法

### 6.1 梯度下降法介绍

**梯度下降法（Gradient Descent）** 是机器学习和深度学习中最核心、最常用的一阶优化算法。其本质是：通过不断迭代，沿着损失函数梯度下降（负梯度）的方向更新模型参数，从而找到使损失函数值**最小**的最优参数解。

**公式：**

```
θ(t+1) = θ(t) - η·∇L(θ(t))
```

| 符号 | 含义 |
|---|---|
| `θₜ+1` | 下一个点（更新后的参数） |
| `θₜ` | 上一个点（当前参数） |
| `η` | 学习率（步长） |
| `∇L(θₜ)` | 对损失函数求偏导（梯度） |

### 6.2 反向传播（了解）

**反向传播（Backpropagation）** 是训练神经网络的核心算法。它通过**微积分中的链式法则（Chain Rule）**，从网络的输出层向输入层逐层计算损失函数对每个参数的梯度（偏导数），然后将这些梯度传递给优化器来更新参数。

对于复合函数 `f(g(x))`，其导数为：

```
df/dx = (df/dg)·(dg/dx)
```

反向传播就是将链式法则应用到神经网络的每一层。设网络有 L 层，梯度是从后往前"接力"传播的——后一层的梯度乘以本层的局部梯度，得到前一层的梯度。

**以单层神经元为例（MSE 损失）：**

设输入为 `x`，权重 `w`，偏置 `b`，激活函数为 `σ`，预测值为 `a`：

1. 损失对输出的导数：`δ = dL/da`
2. 激活函数对线性输入的导数（局部梯度）：`σ'(z)`
3. 损失对 z 的导数（误差信号）：`dL/dz = δ·σ'(z)`
4. 损失对权重 w 的梯度：`dL/dw = (dL/dz)·x`
5. 损失对偏置 b 的梯度：`dL/db = dL/dz`
6. 损失对输入 x 的梯度（传递给前一层）：`dL/dx = (dL/dz)·w`

### 6.3 梯度下降优化

#### 6.3.1 需要优化的原因

- 普通 SGD 在梯度方向变化剧烈时收敛慢
- 学习率难以选择：太大会震荡，太小收敛慢
- 不同参数的梯度尺度不同，需要自适应学习率

#### 6.3.2 前置知识——指数加权平均

指数移动加权平均（Exponential Moving Average, EMA）：

```
vₜ = β·v(t-1) + (1-β)·gₜ
```

其中 `gₜ` 是当前梯度，`β` 是衰减系数（通常 0.9），`vₜ` 是历史梯度的加权平均。

#### 6.3.3 动量法 Momentum

**思想**：累积历史梯度的方向，加速收敛，抑制震荡。

**公式：**

```
vₜ = β·v(t-1) + (1-β)·gₜ
θ(t+1) = θ(t) - η·vₜ
```

**PyTorch 代码：**

```python
optimizer = torch.optim.SGD(model.parameters(), lr=0.01, momentum=0.9)
```

#### 6.3.4 自适应梯度 AdaGrad

**思想**：为每个参数自适应调整学习率，频繁更新的参数学习率变小，不频繁的变大。

**公式：**

```
Gₜ = G(t-1) + gₜ²
```
```
θ(t+1) = θ(t) - [η/√(Gₜ + ε)]·gₜ
```

**优缺点：**
- 优点：自适应学习率，适合稀疏数据
- 缺点：学习率会持续衰减，后期可能过小导致训练提前停止

**PyTorch 代码：**

```python
optimizer = torch.optim.Adagrad(model.parameters(), lr=0.01)
```

#### 6.3.5 均方根传播 RMSProp

**思想**：解决 AdaGrad 学习率持续衰减的问题，使用指数加权平均代替累积平方和。

**公式：**

```
E[g²]ₜ = β·E[g²](t-1) + (1-β)·gₜ²
```
```
θ(t+1) = θ(t) - [η/√(E[g²]ₜ + ε)]·gₜ
```

**PyTorch 代码：**

```python
optimizer = torch.optim.RMSprop(model.parameters(), lr=0.01, alpha=0.9)
```

#### 6.3.6 自适应动量估计 Adam

**思想**：结合 Momentum（一阶矩）和 RMSProp（二阶矩），同时考虑梯度的均值和方差。

**公式：**

```
mₜ = β₁·m(t-1) + (1-β₁)·gₜ
```
```
vₜ = β₂·v(t-1) + (1-β₂)·gₜ²
```

偏差修正：

```
m̂ₜ = mₜ / (1 - β₁ᵗ)
```
```
v̂ₜ = vₜ / (1 - β₂ᵗ)
```

参数更新：

```
θ(t+1) = θ(t) - [η/(√v̂ₜ + ε)]·m̂ₜ
```

通常 `β1=0.9`，`β2=0.999`，`ε=1e-8`。

**优缺点：**
- 优点：自适应学习率、收敛快、对超参数不敏感、是目前最常用的优化器
- 缺点：可能不收敛（在某些任务上泛化不如 SGD+Momentum）

**PyTorch 代码：**

```python
optimizer = torch.optim.Adam(model.parameters(), lr=0.001, betas=(0.9, 0.999))
```

### 6.4 五种优化方法比较

| 优化器 | 核心思想 | 优点 | 缺点 | 适用场景 |
|---|---|---|---|---|
| **SGD** | 沿负梯度更新 | 简单、泛化好 | 收敛慢、对学习率敏感 | 有充足调参时间，追求泛化 |
| **Momentum** | 累积历史梯度 | 加速收敛、抑制震荡 | 多一个超参数 | 大多数场景的基础选择 |
| **AdaGrad** | 自适应学习率（累积） | 适合稀疏数据 | 学习率持续衰减 | NLP、稀疏特征 |
| **RMSProp** | 自适应学习率（指数平均） | 解决 AdaGrad 衰减问题 | 对超参数较敏感 | RNN、在线学习 |
| **Adam** | 结合 Momentum + RMSProp | 收敛快、自适应强 | 泛化可能略差 | 默认首选，大多数任务 |

### 6.5 学习率优化

#### 6.5.1 阶梯衰减（Step Decay / MultiStep Decay）

每隔一定轮数，学习率乘以一个因子（如 0.1）。

```python
scheduler = torch.optim.lr_scheduler.StepLR(optimizer, step_size=30, gamma=0.1)
# 每 30 个 epoch，学习率乘以 0.1
```

#### 6.5.2 指定间隔学习率衰减（MultiStepLR）

在指定的 epoch 节点进行衰减。

```python
scheduler = torch.optim.lr_scheduler.MultiStepLR(optimizer, milestones=[30, 60, 90], gamma=0.1)
# 在第 30、60、90 个 epoch 时，学习率乘以 0.1
```

#### 6.5.3 指数学习率衰减（ExponentialLR）

每个 epoch 学习率乘以 gamma。

```python
scheduler = torch.optim.lr_scheduler.ExponentialLR(optimizer, gamma=0.95)
# 每个 epoch 学习率乘以 0.95
```

#### 6.5.4 三者对比

| 策略 | 衰减方式 | 优点 | 缺点 |
|---|---|---|---|
| **StepLR** | 等间隔衰减 | 简单可控 | 可能错过最佳衰减点 |
| **MultiStepLR** | 指定节点衰减 | 灵活，可根据任务设定 | 需要先验知识 |
| **ExponentialLR** | 每轮指数衰减 | 平滑过渡 | 后期学习率可能过小 |

---

## 七、正则化方法

### 7.1 Dropout 正则化

**Dropout（随机失活）** 是一种常用的正则化技术，在训练过程中以一定概率 p 随机"关闭"部分神经元，防止模型过拟合。

**原理：**
- 训练时：每个神经元以概率 p 被置零（不参与前向和反向传播）
- 测试时：所有神经元都参与，但输出需要乘以 (1-p) 进行缩放（PyTorch 的 `nn.Dropout` 已自动处理）

**PyTorch 代码实现：**

```python
import torch
import torch.nn as nn

class ModelWithDropout(nn.Module):
    def __init__(self, input_dim, hidden_dim, output_dim):
        super().__init__()
        self.linear1 = nn.Linear(input_dim, hidden_dim)
        self.dropout = nn.Dropout(p=0.5)  # 以 50% 概率随机失活
        self.linear2 = nn.Linear(hidden_dim, output_dim)

    def forward(self, x):
        x = torch.relu(self.linear1(x))
        x = self.dropout(x)  # 只在训练时生效
        x = self.linear2(x)
        return x

# 训练和测试模式切换
model.train()  # 训练模式，Dropout 生效
model.eval()   # 评估模式，Dropout 不生效
```

### 7.2 批量归一化 Batch Normalization

**批量归一化（Batch Normalization, BN）** 对每一批数据的每个特征通道进行归一化，使输出均值为 0、方差为 1，再通过可学习的缩放参数 `γ` 和平移参数 `β` 恢复表达能力。

**公式：**

```
x̂ = (x - μ_B) / √(σ_B² + ε)
```
```
y = γ·x̂ + β
```

其中 `μ_B` 和 `σ_B²` 是当前批次的均值和方差，`γ` 和 `β` 是可学习参数。

**优点：**
- 允许使用更大的学习率，加速收敛
- 减少对参数初始化的依赖
- 有一定的正则化效果（类似 Dropout）
- 缓解内部协变量偏移（Internal Covariate Shift）

**PyTorch 代码实现：**

```python
import torch
import torch.nn as nn

class ModelWithBN(nn.Module):
    def __init__(self, input_dim, hidden_dim, output_dim):
        super().__init__()
        self.linear1 = nn.Linear(input_dim, hidden_dim)
        self.bn = nn.BatchNorm1d(hidden_dim)  # 批量归一化
        self.linear2 = nn.Linear(hidden_dim, output_dim)

    def forward(self, x):
        x = self.linear1(x)
        x = self.bn(x)        # BN 通常放在线性层之后、激活函数之前
        x = torch.relu(x)
        x = self.linear2(x)
        return x
```

---

## 八、案例——手机价格分类

### 8.1 需求分析

根据手机的各项特征（如电池容量、摄像头像素、内存等），预测手机的价格区间（4 个类别）。

- 输入：20 个手机特征
- 输出：4 个价格类别
- 任务类型：多分类

### 8.2 代码实现

```python
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import DataLoader, TensorDataset
import time

# 1. 构建分类网络模型
class PhonePriceModel(nn.Module):
    def __init__(self, input_dim, output_dim):
        super().__init__()
        # 隐藏层1
        self.linear1 = nn.Linear(input_dim, 128)
        # 隐藏层2
        self.linear2 = nn.Linear(128, 256)
        # 输出层
        self.output = nn.Linear(256, output_dim)

    def forward(self, x):
        # 隐藏层1: 加权求和 + 激活函数(relu)
        x = torch.relu(self.linear1(x))
        # 隐藏层2: 加权求和 + 激活函数(relu)
        x = torch.relu(self.linear2(x))
        # 输出层: 加权求和（不手动 softmax，CrossEntropyLoss 内部包含）
        x = self.output(x)
        return x

# 2. 训练模型
def train(train_dataset, input_dim, output_dim):
    # 2.1 准备数据加载器
    train_loader = DataLoader(train_dataset, batch_size=16, shuffle=True)

    # 2.2 构建神经网络模型
    model = PhonePriceModel(input_dim, output_dim)

    # 2.3 定义损失函数（多分类交叉熵）
    criterion = nn.CrossEntropyLoss()

    # 2.4 创建优化器
    optimizer = optim.SGD(model.parameters(), lr=0.001)

    # 2.5 模型训练
    epochs = 50
    for epoch in range(epochs):
        total_loss, batch_num = 0.0, 0
        start = time.time()

        for x, y in train_loader:
            # 切换到训练模式
            model.train()
            # 模型预测
            y_pred = model(x)
            # 计算损失
            loss = criterion(y_pred, y)
            # 梯度清零、反向传播、更新参数
            optimizer.zero_grad()
            loss.backward()
            optimizer.step()
            # 累计损失
            total_loss += loss.item()
            batch_num += 1

        avg_loss = total_loss / batch_num
        print(f"Epoch {epoch+1}/{epochs}, Loss: {avg_loss:.4f}, Time: {time.time()-start:.2f}s")

    # 保存模型
    torch.save(model.state_dict(), './model/phone_price_model.pth')
    print('训练结束，模型已保存')

# 3. 测试模型
def evaluate(test_dataset, input_dim, output_dim):
    # 加载模型
    model = PhonePriceModel(input_dim, output_dim)
    model.load_state_dict(torch.load('./model/phone_price_model.pth'))

    test_loader = DataLoader(test_dataset, batch_size=8, shuffle=False)

    correct = 0
    total = 0
    model.eval()
    with torch.no_grad():
        for x, y in test_loader:
            y_pred = model(x)
            _, predicted = torch.max(y_pred, 1)
            total += y.size(0)
            correct += (predicted == y).sum().item()

    accuracy = correct / total
    print(f'测试集准确率: {accuracy:.4f} ({correct}/{total})')
```

### 8.3 网络性能优化

**优化方向：**

1. **增加网络深度**：增加隐藏层数量，提升模型表达能力

```python
class PhonePriceModelV2(nn.Module):
    def __init__(self, input_dim, output_dim):
        super().__init__()
        self.linear1 = nn.Linear(input_dim, 128)
        self.linear2 = nn.Linear(128, 256)
        self.linear3 = nn.Linear(256, 512)
        self.linear4 = nn.Linear(512, 128)
        self.linear5 = nn.Linear(128, output_dim)

    def forward(self, x):
        x = torch.relu(self.linear1(x))
        x = torch.relu(self.linear2(x))
        x = torch.relu(self.linear3(x))
        x = torch.relu(self.linear4(x))
        output = self.linear5(x)
        return output
```

2. **使用 Adam 优化器**：收敛更快
3. **添加 Dropout**：防止过拟合
4. **添加 Batch Normalization**：加速收敛、稳定训练
5. **调整学习率**：使用学习率衰减策略
6. **增加训练轮数**：充分训练

---

## 附录：常用 PyTorch API 速查

| 功能 | API |
|---|---|
| 线性层 | `nn.Linear(in_features, out_features)` |
| Sigmoid | `torch.sigmoid(x)` / `nn.Sigmoid()` |
| Tanh | `torch.tanh(x)` / `nn.Tanh()` |
| ReLU | `torch.relu(x)` / `nn.ReLU()` |
| Softmax | `torch.softmax(x, dim=-1)` / `nn.Softmax(dim=-1)` |
| 二分类交叉熵 | `nn.BCELoss()` / `nn.BCEWithLogitsLoss()` |
| 多分类交叉熵 | `nn.CrossEntropyLoss()` |
| MSE | `nn.MSELoss()` |
| MAE | `nn.L1Loss()` |
| Smooth L1 | `nn.SmoothL1Loss()` |
| SGD | `optim.SGD(params, lr, momentum=0.9)` |
| Adam | `optim.Adam(params, lr=0.001)` |
| Dropout | `nn.Dropout(p=0.5)` |
| 批量归一化 | `nn.BatchNorm1d(num_features)` |
| Xavier 初始化 | `nn.init.xavier_normal_(weight)` |
| Kaiming 初始化 | `nn.init.kaiming_normal_(weight, nonlinearity='relu')` |
| 保存模型 | `torch.save(model.state_dict(), path)` |
| 加载模型 | `model.load_state_dict(torch.load(path))` |
